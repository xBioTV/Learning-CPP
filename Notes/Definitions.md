# A brief word
First, I want to say the purpose of this file is so I can read, and write basic programming terminology.
I will be using the glossary from [Programming: Principles and Practice Using C++ by Bjarne Stroustrup](https://www.stroustrup.com/PPP2e_Glossary.pdf) and the [CPP Reference Site](https://cppreference.com/)
There is also [Bjarne Stroustrup's Personal Glossary](https://www.stroustrup.com/glossary.html) for more advanced terms.

Second, I want to establish a re-useable format that essentially will be reading clarity. It will look something like:

```
Word - Definition
Word in my own words
```

This will probably go through a refining stage to get it how I want it, but structurally this should be good enough for reading clarity.
After typing the A's I'm debating on adding the parenthetical text from the definitions, so that's why some of them have it and some don't.

# Glossary

## A
```
Abstract Class - a class that cannot be directly used to create objects; often used to define an interface to derived classes. A class is made abstract by having a pure virtual function or protected constructor.
a type of class used to interface derived classes, it can't create objects. 
```

```
Abstraction - a description of something that selectively and deliberately ignores details; selective ignorance.
A way to define something that ignores specific details.
```

```
Address - a value that allows us to find an object in a computer's memory.
the location of data from a computer's memory that is a defined value.
```

```
Algorithm - a procedure or formula for solving a problem; a finite series of computational steps to produce a result.
a reproducible set of steps that solve a problem.
```

```
Alias - an alternative way of referring to an object; often a name, pointer, or reference.
another way to reference an object. 
```

```
Application - a program or a collection of programs that is considered an entity by its users.
a way for a user to describe a single-use or multi-use program.
```

```
Approximation - something (e.g., a value or design) that is close to the perfect or ideal (value or design). Often an approximation is a result of trade-offs among ideals.
Something that is close or good enough for the specific intended purpose. 
```

```
Argument - a value passed to a function or a template, in which it is accessed through a parameter.
a way to pass values into a function or template which is used by a parameter.
```

```
Array - a homogeneous sequence of elements, usually numbered, e.g., [0:max].
a list of items, usually starting from 0
```

```
Assertion - a statement inserted into a program to state (assert) that something must always be true at this point in the program.
a way to tell the program that something will be true at a specific point.
```